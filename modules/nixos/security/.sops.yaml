# sops 加密规则：只对 secrets/ 下的 yaml 生效
# 生成 age 密钥后把公钥填入下面 keys 段（age-keygen -y ~/.config/sops/age/keys.txt）
keys:
  - &orion age14v8pr3ucfr9355u2rtqrcg75edjvq3ntd30j3fqryg2jmzccfdhqchkxu7
creation_rules:
  - path_regex: secrets/.*\.yaml$
    key_groups:
      - age:
          - *orion
